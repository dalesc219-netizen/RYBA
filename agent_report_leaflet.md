# Отчет по задаче: Интеграция интерактивной карты Leaflet

## Что было сделано:
1. Файл `App.tsx` был полностью переписан согласно требованиям. Текстовый список `<ul>` (и весь предыдущий кастомный код, если он присутствовал) удален.
2. Внедрены компоненты из `react-leaflet`: `MapContainer`, `TileLayer`, `Polyline`, `Marker`, `Popup`.
3. `MapContainer` настроен на весь экран с использованием классов Tailwind (`h-screen w-full`), установлен центр на координаты `[58.2715, 38.3810]` и `zoom={10}`.
4. Добавлена базовая подложка OpenStreetMap через `<TileLayer>`.
5. Импортированы геоданные `riverBeds` и `historicalPoints` из `src/data/mapData.ts`.
6. С помощью метода `.map()` отрисованы русла рек (`<Polyline>`) с толщиной линии 3 и соответствующими цветами.
7. Аналогичным образом отрисованы маркеры исторических точек (`<Marker>`), внутри каждого добавлен `<Popup>` с названием.
8. Проверено наличие импорта стилей `'leaflet/dist/leaflet.css'` в `main.tsx` — он уже присутствовал и активен.

## Статус:
Код обновлен, сервер должен автоматически подхватить изменения (Hot Module Replacement). Задание выполнено успешно.
